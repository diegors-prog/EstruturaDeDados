package com.company;

import java.util.Scanner;

public class BombaDeGasAlcool {
   /* Scanner tc = new Scanner(System.in);
    public static int tamanho_veiculos = 2;

    private double valor_gasolina = 2.90;
    private double valor_etanol = 2.27;
    String nome [] = new String[tamanho_veiculos];;
    int litrosAbastecidosPorMinuto[] = new int[tamanho_veiculos];
    double carros_etanol[] = new double[tamanho_veiculos];
    double carros_gasolina[] = new double[tamanho_veiculos];
    double totalLitros = 0.0;



    public void bombaAbastecerEtanol(){
        Scanner tc =new Scanner(System.in);
        for (int i=0; i<tamanho_veiculos;i++){
            System.out.println("Digite o nome do carro ");
            nome[i] = tc.nextLine();
            System.out.println("digite quantos litros de etanol deseja abastecer: "+(i+1));
            carros_etanol[i]=Double.parseDouble(tc.nextLine());
        }
        calcularEtanol();
        exibirTotalAbastecido();
    }

    public void calcularEtanol(){
        double valor_etanol =2.27;
        double soma = 0;

        for (int y = 0; y < tamanho_veiculos; y++) {
            soma = soma + carros_etanol[y];
        }
        totalLitros = (valor_etanol * soma);
        System.out.println("O total de litros em etanol é " + totalLitros);
    }

    public void calcularGasolina(){
        double valor_gasolina =2.90;
        double soma = 0;

        for (int y = 0; y < tamanho_veiculos; y++) {
            soma = soma + carros_gasolina[y];
        }
        totalLitros = (valor_gasolina * soma);
        System.out.println("O total de litros em Gasolina é " + totalLitros);
    }



    public void exibirTotalAbastecido() {
        for (int k = 0; k < tamanho_veiculos; k++) {
            if (carros_etanol[k] > totalLitros) {
                // System.out.println("A bomba etanol abasteceu: " + carros_etanol[k]);
                System.out.println(nome[k] + " abasteceu: " + carros_etanol[k]);
            }
        }
    }*/

}

